You're Right - This is Over-Engineering
The multi-collateral stablecoin architecture I proposed is over-engineered for your current needs and creates unnecessary complexity.

Let's Go With Your Recommendation: Option 1
Keep it simple. Build on what's working.

Revised Smart Contract Architecture (Simple & Focused)
typescript
// SOLANA PROGRAMS FOR PHASE 1 (4-6 weeks)
const phase1Programs = {
  1: 'AGENT_STAKING_PROGRAM',     // Migrate database stakes → on-chain PDAs
  2: 'TREASURY_MANAGER_PROGRAM',  // Decentralize treasury management  
  3: 'BURN_GOVERNANCE_PROGRAM',   // On-chain burn approvals
  4: 'TOKEN_2022_WRAPPER',        // Keep current TKOIN but add extensions
};
1. AGENT_STAKING_PROGRAM (Week 1-2)
Purpose: Move agent stakes from database → on-chain PDAs

rust
// Simple stake tracking - no multi-collateral complexity
#[account]
pub struct AgentStake {
    pub wallet: Pubkey,
    pub staked_tkoin: u64,        // Staked TKOIN amount
    pub tier: u8,                  // 1=Bronze, 2=Silver, 3=Gold  
    pub volume_30d: u64,           // Track for tier upgrades
    pub is_active: bool,
}
2. TREASURY_MANAGER_PROGRAM (Week 3-4)
Purpose: Secure treasury with multi-sig PDA control

rust
// Simple treasury - no multi-collateral management
#[account]
pub struct Treasury {
    pub total_tkoin: u64,
    pub burn_reserve: u64,         // Tokens waiting to be burned
    pub agent_rewards: u64,        // Rewards pool
    pub last_burn_timestamp: i64,
}
3. BURN_GOVERNANCE_PROGRAM (Week 5-6)
Purpose: On-chain burn approvals (decentralize the 1% burn)

rust
// Simple burn governance - no complex tokenomics
pub fn propose_burn(ctx: Context<ProposeBurn>, amount: u64) -> Result<()> {
    // Multi-sig approval for treasury burns
    // Transparent on-chain record
    // Community visibility
}
4. TOKEN_2022_WRAPPER (Week 2)
Purpose: Enhance current TKOIN with Token-2022 features

typescript
// Just add extensions to existing TKOIN
const enhancedTkoin = {
  mint: 'J8ambbVjah5R8MSFzW7ykJ3QhcVjh4PTgKGrCzT9wPto', // Your existing mint
  newExtensions: [
    'TransferFee',      // For the 1% burn mechanism
    'MetadataPointer',  // Better wallet support
    'MintCloseAuthority' // Safety feature
  ]
};
